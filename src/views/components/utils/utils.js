const colors = {
    melon: "#F50B92",
    ocean: "#2BF5F1",
    darkGrey: "#383838",
    medGrey: "#707070",
    lightGrey: "#EFEFEF",
    neon: "#DDFA00"
};

const gradients = {
    maoMelon: `linear-gradient(135deg, ${colors.melon} 25%, ${colors.ocean} 105%)`
}

const borderRadius = {
    sm: "5px"
}

const font = {
    spaceMono: "'space mono', 'courier new', 'monotype', sans-serif",
    montserrat: "'montserrat', 'helvetica', sans-serif"
}

export default {
    colors,
    gradients,
    borderRadius,
    font
};