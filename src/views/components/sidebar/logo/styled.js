import styled from "styled-components";
import utils from "../../utils/utils";

export const LogoContainer = styled.div`
  background: ${utils.gradients.maoMelon};
  height: 85px;
  width: 50px;
  border-radius: 5px;
  z-index: 50;
  position: fixed;
  top: 3em;
  left: 3em;
`

export const LogoLink = styled.a`
`